/**
 * Desktop app constants
 *
 * Window and UI-related constants for the Electron main process.
 */

/**
 * Default window dimensions
 */
export const DEFAULT_WINDOW_WIDTH = 1200;
export const DEFAULT_WINDOW_HEIGHT = 800;

/**
 * Position of macOS traffic light buttons (close, minimize, maximize)
 * when using hiddenInset title bar style
 */
export const TRAFFIC_LIGHT_POSITION = { x: 16, y: 16 } as const;
