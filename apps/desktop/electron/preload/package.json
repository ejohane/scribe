{
  "name": "@scribe/electron-preload",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/preload.js",
  "scripts": {
    "build": "node build-simple.mjs",
    "build:watch": "node build.mjs --watch",
    "dev": "node build.mjs --watch",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@scribe/shared": "workspace:*",
    "electron": "^33.2.0"
  },
  "devDependencies": {
    "@scribe/tsconfig": "workspace:*",
    "@types/node": "^22.10.1",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
