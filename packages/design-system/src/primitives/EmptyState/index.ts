/**
 * EmptyState primitive exports
 *
 * Provides consistent empty state styles across the application.
 *
 * Usage:
 * - `emptyStateInline` - For cards/widgets (compact, italic)
 * - `emptyStateCentered` - For floating menus/dropdowns (centered)
 * - `emptyStateFull` - For full-page/section views (flex layout)
 * - `base` + `variants` - For component composition
 */
export {
  base,
  variants,
  emptyStateInline,
  emptyStateCentered,
  emptyStateFull,
} from './EmptyState.css';
