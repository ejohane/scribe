/**
 * Shared constants for Scribe
 *
 * These constants are shared across multiple packages and provide
 * a single source of truth for common values.
 */

/**
 * Default title for newly created notes
 */
export const DEFAULT_NOTE_TITLE = 'Untitled';
