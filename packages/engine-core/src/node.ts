/**
 * Node.js-only exports from engine-core
 *
 * These modules use Node.js APIs (fs, path) and cannot be imported in browser contexts.
 * Import from '@scribe/engine-core/node' instead of the main entry point.
 */

export { TaskIndex } from './task-index.js';
